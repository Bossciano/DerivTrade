kind = "web"
previewPath = "/"
title = "DerivTrade"
version = "1.0.0"
id = "artifacts/derivtrade"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18393

[services.development]
run = "pnpm --filter @workspace/derivtrade run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/derivtrade", "run", "build" ]
publicDir = "artifacts/derivtrade/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18393"
BASE_PATH = "/"
